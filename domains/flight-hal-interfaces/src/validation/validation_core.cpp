/**
 * @file validation_core.cpp
 * @brief Implementation of Core Validation Infrastructure
 */

#include "../../include/flight/hal/validation/validation_core.hpp"
#include <iostream>

namespace flight::hal::validation {

// ValidationReport implementation is header-only

// ValidationUtils static implementations are header-only

// ValidationRegistry implementation is header-only since it's a singleton

} // namespace flight::hal::validation
