{
  "name": "flight-hal-windows-memory",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-windows-memory//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
