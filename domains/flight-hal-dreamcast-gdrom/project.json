{
  "name": "flight-hal-dreamcast-gdrom",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-dreamcast-gdrom//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
