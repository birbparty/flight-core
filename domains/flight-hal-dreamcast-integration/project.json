{
  "name": "flight-hal-dreamcast-integration",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-dreamcast-integration//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
