{
  "name": "flight-hal-time-interface",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-time-interface//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
