{
  "name": "flight-hal-steamos",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-steamos//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
