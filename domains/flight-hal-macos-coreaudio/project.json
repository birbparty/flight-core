{
  "name": "flight-hal-macos-coreaudio",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-macos-coreaudio//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
