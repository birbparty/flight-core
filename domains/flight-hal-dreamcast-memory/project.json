{
  "name": "flight-hal-dreamcast-memory",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-dreamcast-memory//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
