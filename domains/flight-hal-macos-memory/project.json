{
  "name": "flight-hal-macos-memory",
  "projectType": "library",
  "sourceRoot": "teams/flight-hal-macos-memory//src",
  "targets": {
    "build": {
      "executor": "@nrwl/cmake:build",
      "options": {
        "buildDirectory": "build"
      }
    },
    "test": {
      "executor": "@nrwl/cmake:test",
      "options": {
        "testDirectory": "build/tests"
      }
    }
  }
}
